<div class="container">
  <div class="row">
    <% @trips.each do |trip| %>
    <div class="col s6 offset-s3 card blue-grey darken-1">
        <%= link_to trip.name, trip_path(trip), class: "card-title" %>
        <%= trip.destination %>
      </div>
    </div>
    <% end %>
  </div>
</div>